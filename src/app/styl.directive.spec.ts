import { StylDirective } from './styl.directive';

describe('StylDirective', () => {
  it('should create an instance', () => {
    const directive = new StylDirective();
    expect(directive).toBeTruthy();
  });
});
